'use client';

/**
 * Right sidebar component - simple colored layout
 * @returns {JSX.Element} Right sidebar with just color
 */
export default function RightSidebar() {
  return (
    <div className="right-sidebar">{/* Empty - just colored background */}</div>
  );
}

/**
 * Home page component - shows the main feed with full layout
 * Layout (left sidebar, main content, right sidebar) is handled by ClientLayout
 * @returns {JSX.Element} Home page content (content is rendered in MainContent component)
 */
export default function Home() {
  // The actual home content is rendered in MainContent component
  // This page exists to define the route and trigger the full layout
  return null;
}

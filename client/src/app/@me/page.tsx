/**
 * Authenticated user home page - shows the main feed for logged-in users
 * Layout (left sidebar, main content, right sidebar) is handled by ClientLayout
 * @returns {JSX.Element} Authenticated home page content
 */
export default function AuthenticatedHome() {
  // The actual home content is rendered in MainContent component
  // This page exists to define the /@me route for authenticated users
  return null;
}
